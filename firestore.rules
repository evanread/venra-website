rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /posts/{post} {
      allow read: if true;  // Allow public read access to blog posts
      allow write: if false;  // Only allow writes through admin panel/backend
    }
  }
}